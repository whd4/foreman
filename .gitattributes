# Normalise to LF in the repo. The CLI is a shebang script and the character packs are
# JSON read by Node; CRLF in either is noise in every diff a Windows contributor makes.
* text=auto eol=lf
*.png binary
*.jpg binary
